# ========================================
# MAIL CONFIGURATION
# ========================================
# Copy các dòng này vào file .env của bạn

# Mail Driver (smtp, sendmail, mailgun, ses, log)
MAIL_MAILER=smtp

# SMTP Server Settings
MAIL_HOST=103.7.40.50
MAIL_PORT=587
MAIL_ENCRYPTION=tls

# Authentication
MAIL_USERNAME=contact@thevotruyen.vn
MAIL_PASSWORD=EnvA4Fruyw2Q1akbS5pz

# Sender Information
MAIL_FROM_ADDRESS="noreply@thevotruyen.vn"
MAIL_FROM_NAME="${APP_NAME}"

# ========================================
# SAU KHI CẬP NHẬT .env:
# ========================================
# 1. Chạy: php artisan config:clear
# 2. Test: php artisan tinker
#    >> Mail::raw('Test', fn($m) => $m->to('your-email@test.com')->subject('Test'));
# ========================================
